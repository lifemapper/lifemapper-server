NAME 			= opt-lifemapper-pydeps
ARCHIVENAME 	= pydeps
VERSION 		= 1.0.0
RELEASE 		= 0

# pyparsing (matplotlib, pytest-cov
# pytz (cherrypy
# rtree (lmpy)
# 
# setuptools (cherrypy, lmpy)
# six (cherrypy, matplotlib
# wheel (cherrypy-cors)
# zipp (cherrypy
#
# idigbio (?)
# contextlib2 (?)

RPM.EXTRAS = "AutoReq: no"

RPM.FILES =  $(PKGROOT)/*
